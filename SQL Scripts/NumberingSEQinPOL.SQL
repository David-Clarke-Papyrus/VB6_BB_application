
UPDATE tPOL SET POL_SEQ = ROWNUMBER FROM tPOL p JOIN
(SELECT ROW_NUMBER() OVER (PARTITION BY TR_ID
ORDER BY POL_ID) AS 'RowNumber',
      POL_QTYFirm,POL_QTYSS,P_TITLE,POL_SEQ,POL_ID
FROM tPOL JOIN tTR ON POL_TR_ID = TR_ID JOIN tPRODUCT ON POL_P_ID = P_ID) a  ON p.POL_ID = a.POL_ID
